# managed by ansible
# generated for {{ ansible_facts['fqdn'] }}

{% for zone in firewall_nsd_zones %}
zone:
    name: "{{ zone.name }}"
    zonefile: "zones/{{ zone.zonefile }}"
{%  if not loop.last %}

{%  endif %}
{% endfor %}
